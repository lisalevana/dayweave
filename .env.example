# Optional: pasted text and the complete Hong Kong demo work without an API key.
OPENAI_API_KEY=

# When configured, GPT also reads screenshots and less-structured phrasing.
# Deterministic application code still owns travel times, feasibility, opening
# hours, and optimization decisions.
OPENAI_MODEL=gpt-5.6-sol
